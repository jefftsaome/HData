[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hdata"
version = "0.1.0"
description = "Harvester data team — data source implementations"
requires-python = "==3.13.*"
dependencies = [
    "aiohttp>=3.14.1",
    "cryptography>=49.0.0",
    "curl-cffi>=0.15.0",
    "htools",
    "playwright>=1.60.0",
    "pycryptodome>=3.23.0",
    "python-socks[asyncio]>=2.8.0",
    "websockets>=16.0",
]

[project.urls]
Repository = "https://github.com/jefftsaome/HData"

[tool.uv.sources]
htools = { git = "https://github.com/jefftsaome/HTools.git" }

[project.entry-points."data_sources"]
cdp_source = "hdata.sources.leyu_cdp:CDPSource"
ws_source = "hdata.sources.leyu_ws:WSSource"

[tool.setuptools.packages.find]
where = ["."]
include = ["hdata*"]

# 签名 runner（Node 脚本）与 wasm 必须随 wheel 分发，
# 否则 git/pip 安装后 api_sign.sign_path() 报"签名脚本缺失"。
[tool.setuptools.package-data]
"hdata.auth" = ["*.cjs", "*.wasm"]
"hdata.protocol" = ["*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "cython>=3.2.6",
    "pytest>=9.1.1",
    "pytest-asyncio>=0.23.0,!=1.4.0",
    "setuptools>=80.0",
    "wheel>=0.45",
]
